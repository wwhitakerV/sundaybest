/**
 * Local UI state for this slice. Keep server state in `api/` and validation in
 * `schemas/`. Export a hook, never a mutable singleton.
 *
 * Copy this folder to start a feature, then delete what you do not need.
 */
export {};
